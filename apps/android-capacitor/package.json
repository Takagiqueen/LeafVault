{
  "name": "leafvault-android-capacitor",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor Android WebView shell for LeafVault domestic testing.",
  "scripts": {
    "cap": "cap",
    "sync": "cap sync android",
    "open:android": "cap open android",
    "build:debug": "cd android && gradlew.bat assembleDebug",
    "build:release": "cd android && gradlew.bat assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "6.2.1",
    "@capacitor/core": "6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "6.2.1"
  }
}
