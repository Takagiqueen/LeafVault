[pytest]
testpaths = tests
python_files = test_*.py
markers =
    regression: full-chain regression checks before preview releases
    security: authentication, authorization, CSRF, and leakage checks
    sync: encrypted snapshot and incremental sync checks
    pwa: PWA and Service Worker static/runtime-adjacent checks
    slow: slower checks that are still safe for local quality gate runs
